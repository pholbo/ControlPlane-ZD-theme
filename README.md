# ControlPlane-ZD-theme

Zendesk Help Center CSS for the External and Internal help centres. Both centres share a common base stylesheet with per-centre overrides.

## Structure

- `shared/base.css` — styles common to both help centres
- `external/overrides.css` — External help centre specific styles
- `internal/overrides.css` — Internal help centre specific styles
